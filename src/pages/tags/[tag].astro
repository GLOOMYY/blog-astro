<!-- ---
import BaseLayout from "../../layouts/BaseLayout.astro";

export async function getStaticPaths() {
  const allPosts = await Astro.glob('../posts/*.md')
  return [
    { params: { tag:"astro" }, props: { posts: allPosts } },
    { params: { tag:"markdown" }, props: { posts: allPosts } },
    { params: { tag:"primero" }, props: { posts: allPosts } },
    { params: { tag:"segundo" }, props: { posts: allPosts } },
    { params: { tag:"tercero" }, props: { posts: allPosts } },
  ];
}

const { tag } = Astro.params;
const { posts } = Astro.props;
// const filteredPosts = posts.filter((post) => post.fronttmatter.tags?.includes(tag));
---

<BaseLayout>
  <p>Entradas etiquetadas: </p>
</BaseLayout> -->